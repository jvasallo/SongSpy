<center>
<h1>Top Songs on Song Spy</h1>

<table cellpadding="10">
  <tr>
    <th>Song Name</th>
    <th>Album Name</th>
    <th>Artist</th>
    <th>Rating</th>
  </tr>

<% @songs.each do |song| %>
  <tr align="right">
    <td><%= song.name %></td>
    <td><%= song.album.name %></td>
    <td><%= song.artist.name %></td>
    <td><%= song.rating %></td>
  </tr>
<% end %>
</table>
</center>
